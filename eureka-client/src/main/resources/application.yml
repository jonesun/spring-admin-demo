server:
  port: 8763
#  servlet:
#    context-path: /eureka-client

spring:
  application:
    name: eureka-client-1

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: ALWAYS

